import { DatosGraficoBarrasDto } from './datos-grafico-barras-dto';

describe('DatosGraficoBarrasDto', () => {
  it('should create an instance', () => {
    expect(new DatosGraficoBarrasDto()).toBeTruthy();
  });
});
